import { parse, print } from 'graphql';

import { NamespacingExecutor } from './executor';

describe('executor', () => {
	describe('NamespacingExecutor', () => {
		it.todo('namespaces operations');

		it.todo('transforms results');
	});
});
