export const nodeUrl = process.env['WG_NODE_URL']!;

export const serverListenPort = parseInt(process.env['WG_SERVER_PORT']!, 10);

export const serverHost = process.env['WG_SERVER_HOST']!;

export const serverToken = process.env['WG_SERVER_TOKEN']!;
