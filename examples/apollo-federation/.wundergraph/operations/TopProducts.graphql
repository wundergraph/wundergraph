query {
	topProducts: federated_topProducts {
		upc
		name
		price
		reviews {
			id
			body
			author {
				id
				name
				username
				reviews {
					product {
						name
					}
				}
			}
		}
	}
}
